#!/bin/bash

#
# Autogenerated by OTB installation process
# DO NOT MODIFY
#

CURRENT_SCRIPT_DIR=`dirname $0`

if [ -e $CURRENT_SCRIPT_DIR/otbMPIApplicationLauncherCommandLine ]
then
  # Prefer using the launcher inside the script dir
  OTB_CLI_LAUNCHER=$CURRENT_SCRIPT_DIR/otbMPIApplicationLauncherCommandLine
else
  # Use the one from the PATH
  OTB_CLI_LAUNCHER=otbMPIApplicationLauncherCommandLine
fi

# works for install tree
if [ -d $CURRENT_SCRIPT_DIR/../@OTB_INSTALL_APP_DIR@ ]
then
  OTB_APPLICATION_PATH=$CURRENT_SCRIPT_DIR/../@OTB_INSTALL_APP_DIR@:$OTB_APPLICATION_PATH
fi

# export it to make it available to otbMPIApplicationLauncherCommandLine environment
export OTB_APPLICATION_PATH

# start the application
$OTB_CLI_LAUNCHER "$@"
